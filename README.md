# Tasker MAX Bot - Бот для управления задачами

Бот для мессенджера MAX с функционалом управления задачами для команды. Поддерживает создание задач, назначение исполнителей, установку дедлайнов и напоминаний.

## 🎯 Основные возможности

- ✅ Создание задач с названием, описанием и тегами
- 👤 Назначение исполнителей
- ⏰ Установка дедлайнов и напоминаний
- 📋 Просмотр активных задач
- ✔️ Отметка задач как выполненных
- 📦 Архив выполненных задач
- 💾 Хранение в PostgreSQL

## 🚀 Быстрый старт

### Вариант 1: Запуск с Docker Compose (рекомендуется)

```bash
# 1. Клонировать репозиторий
git clone <your-repo>
cd tasker_max_bot

# 2. Запустить все сервисы
docker compose up -d

# 3. Проверить логи
docker compose logs -f backend

# API будет доступен на http://localhost:8000
# База данных на localhost:5432
```

### Вариант 2: Локальный запуск

```bash
# 1. Запустить только БД через Docker
docker compose up -d db

# 2. Установить зависимости Python
pip install -r requirements.txt

# 3. Запустить бота
python bot_with_db.py

# API будет доступен на http://localhost:8000
```

## 📦 Структура проекта

```
tasker_max_bot/
├── bot_with_db.py           # Основной скрипт бота с API
├── test_api.py              # Тестовый скрипт для проверки API
├── requirements.txt         # Python зависимости
├── Dockerfile              # Docker образ для бота
├── docker-compose.yml      # Конфигурация Docker Compose
├── .env.example            # Пример переменных окружения
├── db/
│   └── init/
│       └── 001_schema.sql  # SQL схема базы данных
└── README.md               # Документация
```

## 🗄️ База данных

### Схема таблиц

**chats** - Чаты в мессенджере
- `id` - внутренний ID
- `max_chat_id` - ID чата в MAX
- `name` - название чата
- `created_at` - дата создания

**tasks** - Задачи команды
- `id` - ID задачи
- `chat_id` - ссылка на чат
- `creator_id` - кто создал задачу
- `assignee_id` - исполнитель (опционально)
- `title` - название задачи
- `description` - описание
- `tag` - тег/категория
- `status` - статус (active/completed/archived)
- `created_at` - дата создания
- `deadline` - дедлайн
- `reminder_at` - дата напоминания
- `completed_at` - дата выполнения

## 📡 API Endpoints

### 1. Создать задачу

**POST** `/tasks`

```json
{
  "chat_id": "123456",
  "creator_id": 94717924,
  "title": "Разработать фичу",
  "description": "Добавить функционал напоминаний",
  "tag": "разработка",
  "assignee_id": 94717924,
  "deadline": "2025-11-15T18:00:00",
  "reminder_at": "2025-11-14T18:00:00"
}
```

**Ответ:**
```json
{
  "id": 1,
  "chat_id": 1,
  "creator_id": 94717924,
  "assignee_id": 94717924,
  "title": "Разработать фичу",
  "description": "Добавить функционал напоминаний",
  "tag": "разработка",
  "status": "active",
  "created_at": "2025-11-08T10:00:00",
  "deadline": "2025-11-15T18:00:00",
  "reminder_at": "2025-11-14T18:00:00",
  "completed_at": null
}
```

### 2. Быстрое создание задачи

**POST** `/tasks/quick_create?chat_id=123&creator_id=94717924&title=Test&description=Desc&tag=important`

Упрощенный endpoint для быстрого создания задачи через query параметры.

### 3. Получить задачу по ID

**GET** `/tasks/{task_id}`

### 4. Получить активные задачи чата

**GET** `/chats/{chat_id}/tasks`

Возвращает список всех активных задач, отсортированных по дедлайну.

### 5. Получить архив выполненных задач

**GET** `/chats/{chat_id}/archive`

Возвращает список выполненных и архивированных задач.

### 6. Обновить статус задачи

**PATCH** `/tasks/{task_id}/status`

```json
{
  "status": "completed"
}
```

Доступные статусы:
- `active` - активная задача
- `completed` - выполненная задача (автоматически устанавливается `completed_at`)
- `archived` - архивированная задача

### 7. Отправить сообщение в MAX

**POST** `/send_message`

```json
{
  "user_id": 94717924,
  "text": "Привет!",
  "notify": true,
  "disable_link_preview": false
}
```

## 🧪 Тестирование

Запустите тестовый скрипт для проверки всех функций:

```bash
python test_api.py
```

Скрипт выполнит следующие тесты:
1. Создание задачи
2. Быстрое создание задачи
3. Получение задачи по ID
4. Получение списка активных задач
5. Завершение задачи
6. Проверка архива

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
# MAX Bot Configuration
MAX_BOT_TOKEN=your_token_here
MAX_BOT_BASE_URL=https://botapi.max.ru

# Database Configuration
DATABASE_URL=postgresql://tasker_user:tasker_password@localhost:5432/tasker
POSTGRES_DB=tasker
POSTGRES_USER=tasker_user
POSTGRES_PASSWORD=tasker_password
```

## 📊 Примеры использования

### Создание задачи через curl

```bash
curl -X POST "http://localhost:8000/tasks" \
  -H "Content-Type: application/json" \
  -d '{
    "chat_id": "123456",
    "creator_id": 94717924,
    "title": "Написать документацию",
    "description": "Описать все API endpoints",
    "tag": "документация"
  }'
```

### Получение активных задач

```bash
curl "http://localhost:8000/chats/123456/tasks"
```

### Завершение задачи

```bash
curl -X PATCH "http://localhost:8000/tasks/1/status" \
  -H "Content-Type: application/json" \
  -d '{"status": "completed"}'
```

## 🐳 Docker команды

```bash
# Запуск всех сервисов
docker compose up -d

# Просмотр логов
docker compose logs -f backend

# Остановка всех сервисов
docker compose down

# Перезапуск с пересборкой
docker compose up -d --build

# Подключение к БД
docker exec -it tasker_max_db psql -U tasker_user -d tasker
```

## 📝 SQL запросы для ручной работы с БД

```sql
-- Посмотреть все задачи
SELECT * FROM tasks ORDER BY created_at DESC;

-- Посмотреть активные задачи
SELECT * FROM tasks WHERE status = 'active';

-- Посмотреть задачи с истекающим дедлайном
SELECT * FROM tasks 
WHERE status = 'active' 
  AND deadline < NOW() + INTERVAL '1 day'
ORDER BY deadline;

-- Статистика по тегам
SELECT tag, COUNT(*) as count
FROM tasks
WHERE status = 'active'
GROUP BY tag
ORDER BY count DESC;
```

## 🔍 Проверка работы API

Откройте в браузере: http://localhost:8000

Вы увидите список всех доступных endpoints.

Для интерактивной документации Swagger UI: http://localhost:8000/docs

## 🐛 Устранение проблем

### БД не запускается

```bash
# Проверить логи БД
docker compose logs db

# Пересоздать контейнер БД
docker compose down
docker volume rm tasker_max_bot_pgdata
docker compose up -d db
```

### Backend не подключается к БД

```bash
# Проверить, что БД здорова
docker compose ps

# Проверить логи backend
docker compose logs backend

# Проверить подключение к БД вручную
docker exec -it tasker_max_db pg_isready -U tasker_user
```

## 📚 Дополнительные ресурсы

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [PostgreSQL документация](https://www.postgresql.org/docs/)
- [asyncpg документация](https://magicstack.github.io/asyncpg/)

## 📄 Лицензия

MIT License - см. файл LICENSE

## 👥 Контакты

Для вопросов и предложений создавайте Issues в репозитории.
