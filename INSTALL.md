# 🚀 Установка и запуск MAX Bot Long Polling

## Предварительные требования

- Python 3.8+ (установлено: Python 3.8.20 в conda environment 'max')
- Docker и Docker Compose
- PostgreSQL (через Docker или локально)
- Conda environment 'max' (уже активирован)

## 📦 Шаг 1: Установка зависимостей

```bash
# Активировать conda environment
conda activate max

# Установить Python зависимости
pip install -r requirements.txt
```

Все зависимости уже установлены в вашем окружении! ✅

## 🗄️ Шаг 2: Запуск базы данных

### Вариант А: Через Docker Compose (рекомендуется)

```bash
# Запустить PostgreSQL в Docker
docker compose up -d db

# Или с sudo, если нужны права
sudo docker compose up -d db

# Проверить, что БД запущена
docker ps | grep tasker_max_db
```

### Вариант Б: Проверка существующей БД

```bash
# Если БД уже запущена, проверьте соединение
docker exec -it tasker_max_db psql -U tasker_user -d tasker -c "SELECT 1;"
```

## 🧪 Шаг 3: Проверка настройки

```bash
# Запустить тестовый скрипт
python test_bot_setup.py
```

Этот скрипт проверит:
- ✅ Установку всех зависимостей
- ✅ Подключение к базе данных
- ✅ Наличие необходимых таблиц

## 🤖 Шаг 4: Запуск бота

### Способ 1: Через скрипт запуска (рекомендуется)

```bash
./start_bot.sh
```

Этот скрипт автоматически:
- Активирует conda environment
- Проверит наличие БД
- Установит зависимости (если нужно)
- Запустит бота

### Способ 2: Прямой запуск

```bash
# Активировать окружение
conda activate max

# Запустить бота
python longpolling_bot.py
```

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Токен MAX Bot API
MAX_BOT_TOKEN=f9LHodD0cOLy---sDc0u5izFP25VAGQm2DBuG1SlbyEza4x4iCCKzzx2B2dAeDpdDawHn3hoFWKGl3CVMffI

# URL базы данных
DATABASE_URL=postgresql://tasker_user:tasker_password@localhost:5432/tasker
```

Или установите переменные в shell:

```bash
export MAX_BOT_TOKEN="ваш_токен"
export DATABASE_URL="postgresql://tasker_user:tasker_password@localhost:5432/tasker"
```

## 📝 Проверка работы

После запуска бота вы увидите:

```
🚀 Запуск MAX Bot с Long Polling...
✅ База данных подключена
✅ HTTP клиент инициализирован
📡 Запуск long polling...
```

Бот готов к приему сообщений! Отправьте ему команду `/помощь` в MAX мессенджере.

## 🐛 Решение проблем

### Проблема: "Connect call failed ('127.0.0.1', 5432)"

**Решение:** База данных не запущена.

```bash
sudo docker compose up -d db
# Подождите 5 секунд
sleep 5
python test_bot_setup.py
```

### Проблема: "Permission denied while trying to connect to Docker"

**Решение 1:** Используйте sudo

```bash
sudo docker compose up -d db
```

**Решение 2:** Добавьте пользователя в группу docker

```bash
sudo usermod -aG docker $USER
newgrp docker
```

### Проблема: "Таблица 'tasks' не найдена"

**Решение:** Пересоздайте базу данных

```bash
# Остановить и удалить контейнеры
docker compose down -v

# Запустить заново
docker compose up -d db
sleep 5

# Проверить
python test_bot_setup.py
```

### Проблема: Бот не отвечает на сообщения

**Проверьте:**

1. **Токен правильный?**
   ```bash
   echo $MAX_BOT_TOKEN
   ```

2. **Бот получает обновления?**
   Проверьте логи - должны появляться сообщения "📬 Получено N обновлений"

3. **Доступен MAX API?**
   ```bash
   curl -H "Authorization: $MAX_BOT_TOKEN" https://botapi.max.ru/updates
   ```

## 📊 Структура проекта

```
tasker_max_bot/
├── longpolling_bot.py       # ⭐ Основной бот с long polling
├── bot_with_db.py            # REST API для управления задачами
├── test_bot_setup.py         # Тест настройки
├── start_bot.sh              # Скрипт запуска
├── requirements.txt          # Python зависимости
├── 001_schema.sql            # Схема БД
├── docker-compose.yml        # Docker конфигурация
├── BOT_USAGE.md              # Документация по использованию
└── INSTALL.md                # Эта инструкция
```

## 🚦 Команды бота

После запуска бот поддерживает следующие команды:

- `/задачи` - показать список активных задач
- `/готово {id}` - отметить задачу выполненной
- `/создать {название}` - создать новую задачу
- `/помощь` - показать справку

Подробнее в файле `BOT_USAGE.md`

## 🎯 Что дальше?

1. ✅ Установка завершена
2. ✅ БД запущена
3. ✅ Бот работает
4. 📱 Отправьте боту команду `/помощь` в MAX

## 💡 Полезные команды

```bash
# Посмотреть логи БД
docker compose logs -f db

# Посмотреть запущенные контейнеры
docker ps

# Остановить все
docker compose down

# Запустить все (БД + API)
docker compose up -d

# Перезапустить только БД
docker compose restart db

# Подключиться к БД
docker exec -it tasker_max_db psql -U tasker_user -d tasker
```

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте `test_bot_setup.py` - он покажет что не так
2. Посмотрите логи: `docker compose logs`
3. Проверьте переменные окружения
4. Убедитесь, что MAX Bot API доступен

---

**Версия:** 1.0.0  
**Дата:** 13.11.2025  
**Conda Environment:** max (Python 3.8.20)

