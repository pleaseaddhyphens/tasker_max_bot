# 🤖 MAX Bot Long Polling - Task Manager

Асинхронный бот для мессенджера MAX с поддержкой управления задачами через команды.

## ✨ Особенности

- ✅ **Long Polling** - асинхронное ожидание сообщений от MAX API
- 📝 **Управление задачами** - создание, просмотр и завершение задач
- 💾 **PostgreSQL** - надежное хранение данных
- 🚀 **Простая установка** - готов к работе за 5 минут
- 🐍 **Python 3.8+** - совместим с вашим conda environment

## 📋 Команды бота

| Команда | Описание | Пример |
|---------|----------|--------|
| `/задачи` | Показать список активных задач | `/задачи` |
| `/готово {id}` | Отметить задачу выполненной | `/готово 5` |
| `/создать {название}` | Создать новую задачу | `/создать Написать отчет` |
| `/помощь` | Показать справку | `/помощь` |

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
conda activate max
pip install -r requirements.txt
```

### 2. Запуск базы данных

```bash
sudo docker compose up -d db
```

### 3. Запуск бота

```bash
./start_bot.sh
```

Или:

```bash
python longpolling_bot.py
```

## 📁 Созданные файлы

### Основные файлы

- **`longpolling_bot.py`** ⭐ - Основной бот с long polling
  - Асинхронное получение сообщений от MAX API
  - Обработка команд `/задачи`, `/готово`, `/создать`
  - Работа с PostgreSQL через asyncpg
  - Полное логирование всех операций

- **`demo_bot.py`** 🎬 - Демонстрация работы бота
  - Не требует подключения к MAX API или БД
  - Показывает все возможности бота
  - Интерактивный режим: `python demo_bot.py --interactive`

- **`test_bot_setup.py`** 🧪 - Проверка настройки
  - Проверяет зависимости
  - Тестирует подключение к БД
  - Проверяет схему базы данных

- **`start_bot.sh`** 🚀 - Скрипт запуска
  - Автоматическая активация conda environment
  - Проверка и запуск БД
  - Установка зависимостей при необходимости

### Документация

- **`INSTALL.md`** 📖 - Полная инструкция по установке
- **`BOT_USAGE.md`** 📚 - Документация по использованию
- **`LONG_POLLING_README.md`** 📄 - Этот файл

## 🔧 Как это работает

### Архитектура

```
┌─────────────────┐
│   MAX API       │
│  botapi.max.ru  │
└────────┬────────┘
         │ Long Polling
         │ (HTTP GET /updates)
         ▼
┌─────────────────┐      ┌──────────────┐
│ longpolling_bot │◄────►│  PostgreSQL  │
│    (Python)     │      │   Database   │
└─────────────────┘      └──────────────┘
```

### Long Polling механизм

1. **Получение обновлений**
   ```python
   GET https://botapi.max.ru/updates?offset=X&timeout=60
   ```

2. **Обработка JSON**
   ```json
   {
     "updates": [
       {
         "update_id": 123,
         "type": "message_created",
         "message": {
           "sender": {"id": 94717924},
           "chat": {"id": "123"},
           "body": {"text": "/задачи"}
         }
       }
     ]
   }
   ```

3. **Маршрутизация команд**
   - Извлечение `user_id` и `chat_id`
   - Определение типа команды
   - Вызов соответствующего обработчика

4. **Отправка ответа**
   ```python
   POST https://botapi.max.ru/messages?user_id=94717924
   Body: {"text": "✅ Задача создана!", "notify": true}
   ```

### База данных

**Таблица `chats`:**
- `id` - внутренний ID
- `max_chat_id` - ID чата в MAX
- `name` - название чата

**Таблица `tasks`:**
- `id` - ID задачи
- `chat_id` - ссылка на чат
- `creator_id` - кто создал
- `assignee_id` - кому назначено
- `title` - название
- `description` - описание
- `status` - active/completed/archived
- `deadline`, `completed_at`, `reminder_at` - временные метки

## 📊 Пример использования

### Сценарий 1: Создание и завершение задачи

```
Пользователь → Бот: /создать Подготовить презентацию
Бот → Пользователь: ✅ Задача #1 создана!
                     📝 Подготовить презентацию

Пользователь → Бот: /задачи
Бот → Пользователь: 📋 Ваши активные задачи (1):
                     1. [1] Подготовить презентацию

Пользователь → Бот: /готово 1
Бот → Пользователь: ✅ Задача #1 отмечена как выполненная!
```

## 🧪 Тестирование

### Демонстрация без БД

```bash
python demo_bot.py
```

Показывает полный сценарий работы с задачами.

### Интерактивный режим

```bash
python demo_bot.py --interactive
```

Позволяет вручную тестировать команды.

### Проверка настройки

```bash
python test_bot_setup.py
```

Проверяет:
- ✅ Зависимости установлены
- ✅ БД доступна
- ✅ Таблицы созданы

## 🔐 Конфигурация

### Переменные окружения

```bash
export MAX_BOT_TOKEN="ваш_токен"
export DATABASE_URL="postgresql://user:pass@localhost:5432/tasker"
```

Или создайте `.env`:

```env
MAX_BOT_TOKEN=f9LHodD0cOLy...
DATABASE_URL=postgresql://tasker_user:tasker_password@localhost:5432/tasker
```

## 🐛 Решение проблем

### БД не запускается

```bash
# Проверить логи
docker compose logs db

# Пересоздать
docker compose down -v
docker compose up -d db
```

### Бот не получает сообщения

1. Проверьте токен: `echo $MAX_BOT_TOKEN`
2. Проверьте доступность API: `curl -H "Authorization: $MAX_BOT_TOKEN" https://botapi.max.ru/updates`
3. Проверьте логи бота

### Ошибки Python

```bash
# Проверить окружение
conda activate max
python --version  # Должно быть 3.8+

# Переустановить зависимости
pip install -r requirements.txt --force-reinstall
```

## 📦 Зависимости

- `httpx` - HTTP клиент для работы с MAX API
- `asyncpg` - асинхронный драйвер PostgreSQL
- `asyncio` - асинхронное программирование (встроен в Python)

Не требуется:
- ❌ `maxapi` - реализован собственный long polling
- ❌ Дополнительные библиотеки для бота

## 🚦 Статус проекта

- ✅ Long polling реализован
- ✅ Команды `/задачи`, `/готово`, `/создать` работают
- ✅ Интеграция с PostgreSQL
- ✅ Полное логирование
- ✅ Обработка ошибок
- ✅ Демонстрационный режим
- ✅ Документация

## 💡 Дополнительные возможности

### Расширение функционала

Вы можете легко добавить новые команды:

```python
# В longpolling_bot.py
async def handle_custom_command(user_id: int, chat_id: str, text: str):
    """Ваша команда"""
    # ... логика ...
    await send_message(user_id, "Ответ")

# В process_message()
elif text.startswith('/моя_команда'):
    await handle_custom_command(user_id, chat_id, text)
```

### Интеграция с REST API

Проект также включает `bot_with_db.py` - REST API для управления задачами:

```bash
# Запустить API
python bot_with_db.py

# В другом терминале - запустить long polling бота
python longpolling_bot.py
```

Теперь у вас будет:
- Long polling бот для пользователей
- REST API для интеграций

## 📞 Поддержка

Если возникли вопросы:

1. Прочитайте `INSTALL.md` - подробная инструкция
2. Проверьте `BOT_USAGE.md` - примеры использования
3. Запустите `test_bot_setup.py` - диагностика
4. Запустите `demo_bot.py` - проверка логики

## 🎯 Следующие шаги

1. ✅ Бот установлен и работает
2. 📱 Добавьте бота в MAX мессенджер
3. 💬 Отправьте команду `/помощь`
4. 📝 Создайте первую задачу
5. 🎉 Готово!

---

**Автор:** AI Assistant  
**Дата:** 13.11.2025  
**Версия:** 1.0.0  
**Python:** 3.8.20 (conda environment: max)  
**Статус:** ✅ Готов к использованию

