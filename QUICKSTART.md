# 🚀 Быстрый старт - Tasker MAX Bot

## Шаг 1: Запуск БД и бота

```bash
# Запустить всё одной командой
docker compose up -d

# Проверить, что всё работает
docker compose ps
```

## Шаг 2: Проверка работы

Откройте браузер: http://localhost:8000

Вы должны увидеть JSON с описанием API.

## Шаг 3: Создайте первую задачу

### Вариант А: Через curl

```bash
curl -X POST "http://localhost:8000/tasks" \
  -H "Content-Type: application/json" \
  -d '{
    "chat_id": "123456",
    "creator_id": 94717924,
    "title": "Моя первая задача",
    "description": "Тестовое описание",
    "tag": "тест"
  }'
```

### Вариант Б: Через браузер (быстрый метод)

Откройте: 
```
http://localhost:8000/tasks/quick_create?chat_id=123&creator_id=94717924&title=Тестовая%20задача&description=Описание&tag=test
```

### Вариант В: Через Python

```python
import requests

response = requests.post("http://localhost:8000/tasks", json={
    "chat_id": "123456",
    "creator_id": 94717924,
    "title": "Моя первая задача",
    "description": "Тестовое описание",
    "tag": "тест"
})

print(response.json())
```

## Шаг 4: Просмотрите созданные задачи

```bash
# Получить все задачи чата
curl "http://localhost:8000/chats/123456/tasks"
```

Или откройте в браузере: http://localhost:8000/chats/123456/tasks

## Шаг 5: Завершите задачу

```bash
# Отметить задачу #1 как выполненную
curl -X PATCH "http://localhost:8000/tasks/1/status" \
  -H "Content-Type: application/json" \
  -d '{"status": "completed"}'
```

## Шаг 6: Проверьте архив

```bash
# Посмотреть выполненные задачи
curl "http://localhost:8000/chats/123456/archive"
```

## 🧪 Автоматическое тестирование

Запустите скрипт, который проверит все функции:

```bash
pip install requests
python test_api.py
```

## 🐛 Если что-то не работает

```bash
# Посмотрите логи
docker compose logs -f backend

# Проверьте состояние БД
docker exec -it tasker_max_db psql -U tasker_user -d tasker -c "SELECT COUNT(*) FROM tasks;"

# Перезапустите всё
docker compose down
docker compose up -d --build
```

## 📚 Что дальше?

1. Прочитайте полный README.md для деталей
2. Изучите все endpoints в документации: http://localhost:8000/docs
3. Интегрируйте бота с вашим MAX мессенджером
4. Настройте напоминания и уведомления

## 💡 Полезные команды

```bash
# Остановить всё
docker compose down

# Запустить только БД (для локальной разработки)
docker compose up -d db

# Очистить всё и начать заново
docker compose down -v
docker compose up -d
```

## 📞 Нужна помощь?

Смотрите полную документацию в README.md или создайте Issue в репозитории.
